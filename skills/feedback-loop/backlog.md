# Feedback Loop — accepted backlog

Accepted proposals, newest first, with the hypothesis each tests and the metric that
would prove it worked. Move to shipped-log.json once built.

_(empty — run the monthly review to populate)_
